# Force npm to use legacy peer dependencies resolution
# This is required for many large enterprise monorepos and complex toolchains
legacy-peer-deps=true

# Save exact versions instead of using caret (^) ranges
save-exact=true

# Ignore engine strictness unless explicitly needed
engine-strict=false
